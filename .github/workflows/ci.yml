name: CI
on: [push]
jobs:
  build:
    name: Go Test
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v1

      - name: Build
        run: go build -v .

      - name: Test
        run: go test ./...
